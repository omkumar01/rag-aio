[project]
name = "rag-aio-workspace"
version = "0.1.1"
description = "rag-aio: asynchronous, modular, high-performance RAG platform (uv workspace root)"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "rag-aio-core",
    "rag-aio-observe",
    "rag-aio-cache",
    "rag-aio-db-handler",
    "rag-aio-doc-handler",
    "rag-aio-ocr",
    "rag-aio-embedder",
    "rag-aio-retrieval",
    "rag-aio-rerank",
    "rag-aio-query",
    "rag-aio-context",
    "rag-aio-llm-provider",
    "rag-aio-generation",
    "rag-aio-orchestrator",
    "rag-aio-mass-inject",
    "rag-aio-eval",
    "rag-aio-ui",
    "rag-aio",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
rag-aio-core = { workspace = true }
rag-aio-observe = { workspace = true }
rag-aio-cache = { workspace = true }
rag-aio-db-handler = { workspace = true }
rag-aio-doc-handler = { workspace = true }
rag-aio-ocr = { workspace = true }
rag-aio-embedder = { workspace = true }
rag-aio-retrieval = { workspace = true }
rag-aio-rerank = { workspace = true }
rag-aio-query = { workspace = true }
rag-aio-context = { workspace = true }
rag-aio-llm-provider = { workspace = true }
rag-aio-generation = { workspace = true }
rag-aio-orchestrator = { workspace = true }
rag-aio-mass-inject = { workspace = true }
rag-aio-eval = { workspace = true }
rag-aio-ui = { workspace = true }
rag-aio = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-PyYAML",
    "httpx>=0.27",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["packages"]

[tool.ruff.lint]
select = [
    "E", "W",   # pycodestyle
    "F",        # pyflakes
    "I",        # isort
    "UP",       # pyupgrade
    "B",        # bugbear
    "SIM",      # simplify
    "C4",       # comprehensions
    "ASYNC",    # async hygiene
    "RUF",      # ruff-specific
]
ignore = [
    "E501",     # long lines allowed in docstrings/comments; formatter handles code
]

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = ["B011", "SIM117"]
"scripts/**" = ["T201"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
show_error_codes = true
pretty = true
mypy_path = ["packages", "examples"]
explicit_package_bases = true
namespace_packages = true

[[tool.mypy.overrides]]
module = [
    "fastembed.*", "fitz.*", "pymupdf.*", "docling.*", "sentence_transformers.*",
    "qdrant_client.*", "faiss.*", "streamlit.*", "docx.*", "openpyxl.*",
    "pptx.*", "extract_msg.*", "striprtf.*", "bs4.*", "lxml.*",
    "sqlalchemy.*", "aiosqlite.*", "asyncpg.*", "redis.*", "diskcache.*",
    "google.*", "anthropic.*", "tokenizers.*", "bm25s.*", "numpy.*",
    "pydantic_ai.*", "uvicorn.*", "opentelemetry.*", "email.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["packages"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = "-q --strict-markers --import-mode=importlib"
markers = [
    "unit: fast tests without external services",
    "integration: tests against real local backends",
    "e2e: end-to-end tests, may require local LM Studio/Qdrant",
    "benchmark: performance benchmarks, run explicitly via `pytest benchmarks -m benchmark`",
]

[tool.coverage.run]
source = ["packages"]
omit = ["*/tests/*"]
