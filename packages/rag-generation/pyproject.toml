[project]
name = "rag-generation"
version = "0.1.0"
description = "rag-aio provider-agnostic LLM generation layer"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["rag-core", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_generation"]
