[project]
name = "rag-aio-orchestrator"
version = "0.1.0"
description = "rag-aio strategy and workflow-management orchestration layer"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "rag-aio-core",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "rag-aio-doc-handler",
    "rag-aio-embedder",
    "rag-aio-db-handler",
    "rag-aio-retrieval",
    "rag-aio-rerank",
    "rag-aio-query",
    "rag-aio-context",
    "rag-aio-generation",
    "rag-aio-llm-provider",
    "rag-aio-cache",
    "rag-aio-observe",
]

[project.optional-dependencies]
pydantic-ai = ["pydantic-ai>=0.2"]
fastapi = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_orchestrator"]
