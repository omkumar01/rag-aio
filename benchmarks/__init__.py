"""rag-aio benchmark suite (Wave 8).

Entry point: ``uv run pytest benchmarks -m benchmark``. See ``benchmarks/README.md``
for the methodology; scenario sources live in ``test_scenarios.py``, the harness
itself in ``harness.py``, and its unit tests in ``test_harness.py``.
"""
