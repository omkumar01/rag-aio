# Modules

Each package under `packages/` has a documentation page here covering: purpose,
responsibilities, public API, configuration, dependencies, service endpoints, data flow,
performance characteristics, failure modes, extension points, examples, testing strategy,
and operational guidance.

Pages are added as modules are implemented; module READMEs under `packages/<name>/README.md`
are the source of truth and these pages link to them.
